"use client";

import { AppShell, SectionPlaceholder } from "@/components/app-shell";

export default function MaintenancePage() {
  return <AppShell title="Maintenance">{() => <SectionPlaceholder title="Maintenance" />}</AppShell>;
}
