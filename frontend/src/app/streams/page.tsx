"use client";

import { AppShell, SectionPlaceholder } from "@/components/app-shell";

export default function StreamsPage() {
  return <AppShell title="Streams">{() => <SectionPlaceholder title="Streams" />}</AppShell>;
}
