"use client";

import { AppShell, SectionPlaceholder } from "@/components/app-shell";

export default function TrendsPage() {
  return <AppShell title="Trends">{() => <SectionPlaceholder title="Trends" />}</AppShell>;
}
