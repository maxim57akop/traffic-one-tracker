"use client";

import { AppShell, SectionPlaceholder } from "@/components/app-shell";

export default function IntegrationsPage() {
  return <AppShell title="Integrations">{() => <SectionPlaceholder title="Integrations" />}</AppShell>;
}
