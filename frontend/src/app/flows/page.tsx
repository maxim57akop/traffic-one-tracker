"use client";

import { AppShell, SectionPlaceholder } from "@/components/app-shell";

export default function FlowsPage() {
  return <AppShell title="Flows">{() => <SectionPlaceholder title="Flows" />}</AppShell>;
}
